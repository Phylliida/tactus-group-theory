once we have our zfc group via aanderaa-cohen + Higman (which we should properly finish as that's the "traditional" approach), we want to decrease overhead (it has tower/expoenential overhead) for proof length.

We can do this by using S-Machines (Sapir-Birget-Rips) and the polynomially-distorted Higman embeddings of Birget-Olshanskii-Sapir.

see reading in ~/prog/verus-cad/after-zfc-group-reading (you can use nix-shell to grab stuff you need to read that)

