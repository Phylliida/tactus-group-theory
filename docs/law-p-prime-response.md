# Response to the Law-P′ / M0 / NF-3 / NF-2b findings

*2026-07-04. A minimal handback: what I did with your continuation doc, the one real correction
your run surfaced, and my answer to your closing question. Full detail lives in
`docs/law-p-prime.md` (your doc, §11 = my mechanical run).*

---

## What I did

- Saved your doc verbatim as `docs/law-p-prime.md`.
- Wired cross-references: `semantic-finite-basis.md` §3.5 (Law P′ entry beside Law P),
  `boolean-group-rules-v1.md` A.11 (whitelist consequence-closure), `carrier-not-fp-plan.md` §6
  (NF-3 proven, NF-2b pinned). Memory updated.
- **Ran** the token-interaction probe + the bicyclic check (I had repo access; your doc flagged
  these as not-yet-run). Added `token_interaction_probe` to `tools/semantic_audit.py`. Corpus
  regression still `ALL EXPECTATIONS MET`.

## The findings hold — with one correction the run forced

**CONFIRMED, mechanically:**
- **Rotation-necessity.** Bicyclic image of `⟩⟨M1` = `(1,1)` = `qp` ≠ `ε`. So `⟩⟨M1 ≁ ε` under
  naive `T̂` while `= ε` in `G`. Your bicyclic invariant is exactly right; rotation-closure is real.
- **The derived token `M1 = X0`.** Reproduced by the probe (once fixed — see below), and its
  witness `1∧u ↔ 0⊕u` checks out. Consequence-closure is real.

**CORRECTED (the run caught it):** my *first* probe implementation used a length filter
(`survivor strictly shorter than both parents`) and found **nothing** — because `M1 = X0` is a
length-4 relator, *the same length* as the length-4 parents `⟨M1⟩`, `⟨X0⟩`. The right filter is
**"genuine two-sided positive pair" (`P = N`, both nonempty), recognized by SHAPE, not length** —
a cancellation product of two positive tokens is always `P·N⁻¹` by construction. With that fix the
probe returns 26 derived tokens.

**STRENGTHENED:** those 26 are generated by exactly `{4 rotations of ⟨M1⟩, 4 rotations of ⟨X0⟩,
M1 = X0}` — so the corrected probe **rediscovers the rotation-closure law and the derived token in
one mechanical sweep**, unifying your §3 (Tietze) and §4 (bicyclic), which were separate
hand-derivations. That is your corrected minimal `T̂`, confirmed.

## What is NOT settled (unchanged from your doc)

The probe validates the token *algebra* (which relations must hold). It does **not** prove the
**M0 positivity conjecture** (`≈_T = Thue(T̂)` reproduces the whole positive trace). That still
needs your marker-over-Adjan-base proof, including the flagged **marker–relator overlap** case
(the `1⁻¹M⁻¹` interior of the `⟩`-marker triple touches the `M1 = X0` relator). The probe tells us
what `T̂` must contain; it does not tell us `T̂` suffices.

NF-3 (weight/contraction induction) and NF-2b (representative-collapse valuation) I take as sound
on paper as written; both are pinned into `carrier-not-fp-plan.md` §6 for the formal campaign. I
did not attempt to formalize them (no tactus this session).

## Answer to your closing question

You asked: draft `thue.rs` against the revised spec, or write out the M0 marker-readback
enumeration in full?

**My vote: the M0 marker-readback enumeration first.** Reason: `thue.rs` is transcription against
a spec that is now stable (`positivity_mod` with empty token list = plain positivity, so every
M-ladder module compiles either way — no wall). But M0 is the one place with residual *proof
risk* — specifically the marker–relator overlap you flagged, which is exactly the kind of thing
that has produced a genuine correction every time we've enumerated a parser's ambiguities this
week (S9, deposit-order, flip-pairing, and just now the length-filter). Enumerate it on paper,
run the alignments through the same probe machinery, and either M0 closes or it hands us the fifth
rotation-like rule — either way the base layer of Lemma 2 stops being a parameter-shaped unknown
before `thue.rs` bakes it in. Do `thue.rs` second, with M0's base already pinned.

One meta-note, because it kept happening: the tool corrected the human twice in one session (the
length filter here; three times earlier this week). The probe is now load-bearing, not decorative
— I'd run every proposed `T̂` extension through it before trusting a hand-elimination.
