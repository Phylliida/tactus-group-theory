# thue-rs-phase0.md — agent draft (Fable), archived

*The agent's blind Phase-0 draft (thue.rs + m0.rs) as received 2026-07-04. RESPONSE +
CORRECTIONS: `docs/thue-rs-phase0-response.md` (the crate already has the substrate verified;
first real module `src/m0_token.rs` compiled 2/0). Archived for the admit-ledger (§4) and the
rules.json single-source proposal (§5), both still live. The draft's soundness-side admits
(A3/A4/step_sound) dissolve into `lemma_hom_preserves_equiv` reuse; A5/A6 survive.*

See `docs/thue-rs-phase0-response.md` for the reuse map and what was built.
